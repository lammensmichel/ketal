import { NgModule } from '@angular/core';
import { NgbModal, NgbModule } from '@ng-bootstrap/ng-bootstrap';

/**
 * NgbModule for ng-bootstrap modals
 */
@NgModule({
  imports: [NgbModule],
  exports: [NgbModule],
})
export class NgbCustomModule {}
