name: Build and Deploy Angular App

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 18.13.0 # Utilisez la version de Node.js souhaitée

      - name: Install dependencies
        run: npm install

      - name: Build Angular app
        run: npm run build

  deploy:
    name: Deploy
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Deploy to Hosting Service
        run: |
          # Ajoutez ici vos commandes de déploiement spécifiques à votre service d'hébergement

# Ajoutez d'autres étapes personnalisées au besoin, comme la configuration des variables d'environnement, etc.
